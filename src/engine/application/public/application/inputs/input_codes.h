#pragma once

namespace application::inputs
{
enum class EButtons
{
    Keyboard_A,
    Keyboard_B,
    Keyboard_C,
    Keyboard_D,
};
enum class EMouseAxis
{
    X,
    Y,
};
} // namespace application::inputs